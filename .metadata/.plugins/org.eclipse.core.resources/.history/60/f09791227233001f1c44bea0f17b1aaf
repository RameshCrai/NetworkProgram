package Project;

import org.apache.commons.validator.routines.InetAddressValidator;

public class ValidationX {
	private static final String INET4ADDRESS = "AAA.8.9.28";

	// an IPv6 address
	private static final String INET6ADDRESS = "2001:0db8:85a3:0000:0000:8a2e:0370:7334";

	public static void main(String[] args) {
		// Get an `InetAddressValidator`
		InetAddressValidator validator = InetAddressValidator.getInstance();

		if (validator == null) {
			System.err.println("Failed to get an instance of InetAddressValidator.");
			return;
		}

		// Validate an IPv4 address
		if (validator.isValidInet4Address(INET4ADDRESS)) {
			System.out.println("The IP address " + INET4ADDRESS + " is valid");
		} else {
			System.out.println("The IP address " + INET4ADDRESS + " isn't valid");
		}

		// Validate an IPv6 address
		if (validator.isValidInet6Address(INET6ADDRESS)) {
			System.out.println("The IP address " + INET6ADDRESS + " is valid");
		} else {
			System.out.println("The IP address " + INET6ADDRESS + " isn't valid");
		}
	}
}
